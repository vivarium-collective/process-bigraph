[project]
name = "process-bigraph"
version = "0.0.39"
description = ""
readme = "README.md"
requires-python = "==3.12.9"
dependencies = [
    "bigraph-schema",
    "matplotlib",
    "pytest"
]

[tool.setuptools]
packages = [
    "process_bigraph"
]

[tool.uv.sources]
bigraph-schema = { path = "../bigraph-schema", editable = true }
