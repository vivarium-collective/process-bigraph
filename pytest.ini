[pytest]
python_files = *.py
python_functions = test*
markers =
    slow: tests that take significant wall-time (Ray cluster spin-up, REST/FastAPI, large simulations). Skipped by default; opt in with --run-slow.
